# PostgreSQL Configuration
PG_DATABASE_NAME=Daily-routine-postgres
PG_USER=root
PG_PASSWORD=123456
PG_PORT=5432
MIGRATION_DIR=./migrations

# PostgreSQL DSN - используйте внутренний хост для подключения из контейнера
# Внутренний хост (для контейнера): dailyroutine-pg-8m9bvb
# Внешний хост (для внешних подключений): 89.111.163.253
PG_DSN="host=dailyroutine-pg-8m9bvb port=5432 dbname=Daily-routine-postgres user=root password=123456 sslmode=disable"

# HTTP Server Configuration
# HTTP_HOST должен быть 0.0.0.0 для прослушивания на всех интерфейсах внутри контейнера
HTTP_HOST=0.0.0.0
# HTTP_PORT должен совпадать с EXPOSE в Dockerfile (8000)
HTTP_PORT=8000

# Telegram Bot Token
TELEGRAM_BOT_TOKEN=8248908460:AAFC0OhLPeu2r09gh_A7fecwMWErMCD5WBw

# JWT Secret
JWT_SECRET=efefoerkjofjoeirnggj859hgi5hbg

# CORS Configuration (если нужно)
ALLOWED_ORIGIN=*

# Test Mode
ENABLE_TEST_MODE=false
TEST_USER_ID=123456789

